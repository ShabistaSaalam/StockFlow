name: inventory_flutter
description: Minimal Flutter frontend for Inventory Allocation System
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 # for making HTTP requests

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
